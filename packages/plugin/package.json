{
  "name": "frida",
  "version": "1.0.0",
  "description": "Main plugin for Attack on Frida",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npm run build",
    "start": "frida -U Gadget -l build/_index.js",
    "watch": "frida-compile index.js -o build/_index.js -w",
    "build": "frida-compile index.js -o build/_index.js -c"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@attack/core": "^1.0.0",
    "@attack/hook-item-http": "^1.0.0"
  },
  "devDependencies": {
    "@types/frida-gum": "^16.2.0",
    "frida-compile": "^19.0.5",
    "jshint": "^2.9.2"
  }
}
